#include <stdio.h>

int main() {
	int year;
	scanf_s("%d", &year);

	int leap;

	if (year % 400 == 0) leap = 1;
	else if (year % 100 == 0) leap = 0;
	else if (year % 4 == 0) leap = 1;
	else leap = 0;

	printf("%d\n", leap);

	return 0;
}
