#include <stdio.h>

int main() {
	int n;
	scanf_s("%d", &n);

	int min = 1000000;
	int max = -1000000;
	int input;

	for (int i = 0; i < n; i++) {
		scanf_s("%d", &input);
		if (input < min) min = input;
		if (input > max) max = input;
	}
	printf("%d %d\n", min, max);

	return 0;
}
